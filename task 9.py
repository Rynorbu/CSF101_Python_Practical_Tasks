#Task 9
# Create a variable called age of type int and assign the value 18 to the variable.
# Print the value of the variable as the output.
# Create a variable called name and assign the value of “dorji” to the variable name
# What is the type of the variable name?
# Print the value of the variable name to the output
 
age = 18
print(age)

name = "dorji"
print(type(name))  # the type of the variable name is string which contains characters
print(name)
